package `2022_template`

import java.io.File

fun first(filename: String) {
    val input : List<String> = File(filename).readLines()
}

first("test.in")
first("first.in")


fun second(filename: String) {
    val input : List<String> = File(filename).readLines()
}

second("test.in")
second("first.in")
